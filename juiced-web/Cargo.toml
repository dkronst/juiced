[package]
name = "juiced-web"
version = "0.1.0"
edition = "2021"

[dependencies]
juiced-status = { path = "../juiced-status" }
tiny_http = "0.12"
serde_json = "1"
log = "0.4"

[dev-dependencies]
ureq = { version = "2", default-features = false }
env_logger = "0.11"
